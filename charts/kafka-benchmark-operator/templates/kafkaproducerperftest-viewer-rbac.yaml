apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kafka-benchmark-operator.fullname" . }}-kafkaproducerperftest-viewer-role
  labels:
  {{- include "kafka-benchmark-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - kafka.platformatory.io
  resources:
  - kafkaproducerperftests
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kafka.platformatory.io
  resources:
  - kafkaproducerperftests/status
  verbs:
  - get
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kafka-benchmark-operator.fullname" . }}-controller-manager
  labels:
  {{- include "kafka-benchmark-operator.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.controllerManager.serviceAccount.annotations | nindent 4 }}